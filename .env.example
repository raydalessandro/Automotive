# Supabase — pipeline lead (§6). Senza queste, il form non salva ma il sito builda e gira.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Notifica lead istantanea via Telegram (§6.2)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Email: fallback notifica + email di cortesia al lead (§6.3), via Resend
RESEND_API_KEY=
LEAD_NOTIFY_EMAIL=

# Base URL pubblico (per link OG/WhatsApp). In locale: http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
